{
  "name": "inspector-jake",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server + Chrome extension for inspecting web pages",
  "repository": "github:inspectorjake/inspector-jake",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @inspector-jake/shared && npm run build -w @inspector-jake/chrome-extension -w inspector-jake-mcp",
    "dev": "npm run dev --workspaces --parallel",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
